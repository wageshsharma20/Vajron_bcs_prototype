#!/usr/bin/env bash
# Put the GCS kiosk back up (no code needed — locking down is never the risky
# direction). Also re-arms it for the next boot.
set -euo pipefail
sudo systemctl start vajron-gcs-kiosk.service
echo "Kiosk running. The GCS is back on screen."
